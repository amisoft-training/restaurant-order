server:
  port: 8001

spring:
  application:
    name: restaurant-order
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/trainingorder?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka



